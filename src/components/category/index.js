import React from "react";
import MultiItemCarousel from "../multi_item_carousel";

function Categories() {
    return (
        <section className="Categories">
            <MultiItemCarousel type="Category"/>
        </section>
    );
}

export default Categories;